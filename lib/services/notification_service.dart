class NotificationService {
  Future<void> initialize() async {}

  Future<void> showLocal(String title, String message) async {
    // Hook this into Firebase Cloud Messaging or flutter_local_notifications for production push.
  }
}
